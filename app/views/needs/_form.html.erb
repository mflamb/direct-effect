<%= form_with(model: need, local: true) do |form| %>
  <% if need.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(need.errors.count, "error") %> prohibited this need from being saved:</h2>

      <ul>
      <% need.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= form.label :organization_id, style: "#{'display:none' if current_user}" %>
    <%= form.text_field :organization_id, class: 'form-control', style: "#{'display:none' if current_user}" %>
  </div>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :enabled %>
    <%= form.check_box :enabled %>
  </div>

  <div class="actions">
    <%= form.submit "Save", class: 'btn btn-primary' %>
  </div>
<% end %>
